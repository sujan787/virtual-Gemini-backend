name: CICD

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_HUB_USER_NAME }} -p ${{ secrets.DOCKER_HUB_ACCESS_TOKEN}}
      - name: Build Docker Backed Image
        run: docker build -t sujan9748/virtual_gemini_backend .
      - name: Publish Backend image to Docker Hub
        run: docker push sujan9748/virtual_gemini_backend:latest

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Remove all stopped containers
        run: docker container prune -f
      - name: Remove all unused images
        run: docker image prune -f
      - name: Pull Node Image from the Docker Hub 🙂
        run: docker pull sujan9748/virtual_gemini_backend:latest
      - name: Delete Old Node Container
        run: docker rm -f GeminiNodeContainer
      - name: Run docker Node container
        run: docker run -d -p 3000:3000 --name GeminiNodeContainer sujan9748/virtual_gemini_backend
      - name: Remove all stopped containers
        run: docker container prune -f
      - name: Remove all unused images
        run: docker image prune -f
